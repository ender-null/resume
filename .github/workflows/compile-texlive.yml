name: Compile (TeX Live)

on:
  push:
    branches:
      - main

  pull_request:

jobs:
  english:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: Build PDF
        run: |
          docker run --mount src=$PWD,target=/docs,type=bind texlive/texlive /bin/sh -c "cd /docs; latexmk -pdflua -f resume.tex"
          mv resume.pdf AlinNedelcu_resume.pdf

      - name: Generate artifact for PDF (english)
        uses: actions/upload-artifact@main
        with:
          name: AlinNedelcu_resume.pdf
          path: AlinNedelcu_resume.pdf

  spanish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: Build PDF
        run: |
          docker run --mount src=$PWD,target=/docs,type=bind texlive/texlive /bin/sh -c "cd /docs; latexmk -pdflua -f resume-spanish.tex"
          mv resume-spanish.pdf AlinNedelcu_curriculum.pdf

      - name: Generate artifact for PDF (spanish)
        uses: actions/upload-artifact@main
        with:
          name: AlinNedelcu_curriculum.pdf
          path: AlinNedelcu_curriculum.pdf

  complete:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: Build PDF
        run: |
          docker run --mount src=$PWD,target=/docs,type=bind texlive/texlive /bin/sh -c "cd /docs; latexmk -pdflua -f resume-complete.tex"
          mv resume-complete.pdf AlinNedelcu_resume_complete.pdf

      - name: Generate artifact for PDF (complete)
        uses: actions/upload-artifact@main
        with:
          name: AlinNedelcu_resume_complete.pdf
          path: AlinNedelcu_resume_complete.pdf

  dev:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: Build PDF
        run: |
          docker run --mount src=$PWD,target=/docs,type=bind texlive/texlive /bin/sh -c "cd /docs; latexmk -pdflua -f resume-dev.tex"
          mv resume-dev.pdf AlinNedelcu_resume_dev.pdf

      - name: Generate artifact for PDF (dev)
        uses: actions/upload-artifact@main
        with:
          name: AlinNedelcu_resume_dev.pdf
          path: AlinNedelcu_resume_dev.pdf
